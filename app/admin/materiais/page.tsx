import { Download, FileText, MoreVertical, Plus, UploadCloud, Users } from "lucide-react";
import { PageHeader } from "@/components/admin/page-header";
import { Button } from "@/components/ui/button";
const rows = [["Checklist do embarque seguro","Checklist","428","Publicado"],["Guia de carreira aeromédica","E-book","316","Publicado"],["Estabilização pré-voo","PDF","148","Rascunho"]];
export const metadata = { title: "Materiais gratuitos" };
export default function MaterialsManager() { return <main><PageHeader eyebrow="Captação de leads" title="Materiais gratuitos" description="Envie PDFs e e-books, controle a publicação e acompanhe quem solicitou cada material." action={<Button><Plus className="size-4" /> Novo material</Button>} /><section className="mb-5 grid min-h-44 place-items-center rounded-2xl border-2 border-dashed border-slate-300 bg-white p-7 text-center"><div><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-blue-50 text-[#1175d1]"><UploadCloud /></span><strong className="mt-4 block">Arraste um arquivo ou clique para selecionar</strong><p className="mt-2 text-xs text-slate-400">PDF de até 20 MB. A capa pode ser enviada separadamente.</p><Button variant="outline" size="sm" className="mt-4">Selecionar arquivo</Button></div></section><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-5"><h2 className="font-bold">Biblioteca publicada</h2></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-slate-50 text-[10px] uppercase tracking-[.14em] text-slate-400"><tr>{["Material","Formato","Downloads","Status",""].map((head) => <th key={head} className="px-5 py-3">{head}</th>)}</tr></thead><tbody>{rows.map(([name,type,downloads,status]) => <tr key={name} className="border-t border-slate-100"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-blue-50 text-[#1175d1]"><FileText className="size-4" /></span><strong className="text-xs">{name}</strong></div></td><td className="px-5 py-4 text-xs text-slate-500">{type}</td><td className="px-5 py-4"><span className="flex items-center gap-2 text-xs"><Download className="size-4 text-slate-400" />{downloads}</span></td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-[10px] font-bold ${status === "Publicado" ? "bg-emerald-50 text-emerald-600" : "bg-amber-50 text-amber-700"}`}>{status}</span></td><td className="px-5 py-4"><button aria-label={`Ações de ${name}`}><MoreVertical className="size-4 text-slate-400" /></button></td></tr>)}</tbody></table></div><div className="flex items-center gap-2 border-t border-slate-100 p-4 text-xs text-slate-500"><Users className="size-4" /> Os leads vinculados ficam disponíveis na área Leads.</div></section></main>; }
