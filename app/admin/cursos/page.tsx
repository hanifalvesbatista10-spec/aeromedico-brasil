import { Clock3, Edit3, Eye, Plus, ShoppingCart, Trash2 } from "lucide-react";
import { PageHeader } from "@/components/admin/page-header";
import { Button } from "@/components/ui/button";
import { courses } from "@/data/site";
import { formatCurrency } from "@/lib/utils";
export const metadata = { title: "Gerenciar cursos" };
export default function CoursesManager() { return <main><PageHeader eyebrow="Catálogo comercial" title="Cursos e produtos" description="Cadastre produtos, defina preço, cronograma, capa e destino do checkout." action={<Button><Plus className="size-4" /> Novo curso</Button>} /><div className="grid gap-5 lg:grid-cols-2 2xl:grid-cols-3">{courses.map((course, index) => <article key={course.id} className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className={`relative h-40 p-5 text-white ${index === 1 ? "bg-[#0e4268]" : index === 2 ? "bg-[#14577a]" : "bg-[#07182b]"}`}><span className="text-[10px] font-bold uppercase tracking-[.18em] text-[#8fe9ff]">{course.badge}</span><h2 className="display-font absolute bottom-5 max-w-xs text-2xl font-black uppercase leading-none">{course.title}</h2></div><div className="p-5"><div className="flex items-center justify-between"><strong className="text-xl">{formatCurrency(course.price)}</strong><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-[10px] font-bold text-emerald-600">Publicado</span></div><div className="mt-4 flex flex-wrap gap-4 text-xs text-slate-500"><span className="flex items-center gap-2"><Clock3 className="size-4" />{course.duration}</span><span className="flex items-center gap-2"><ShoppingCart className="size-4" /> Checkout preparado</span></div><div className="mt-5 grid grid-cols-3 gap-2"><Button variant="outline" size="sm"><Edit3 className="size-3.5" /> Editar</Button><Button variant="ghost" size="sm"><Eye className="size-3.5" /> Ver</Button><Button variant="ghost" size="sm" className="text-red-500 hover:bg-red-50 hover:text-red-600"><Trash2 className="size-3.5" /> Excluir</Button></div></div></article>)}</div></main>; }
